# 260313-2 결제 관련 인지부채 분석

## CRITICAL - 즉시 해결 필요

### 1. 상품 ID 불일치 (4곳에서 다른 ID 사용)
| 파일 | 상품 ID | 비고 |
|------|--------|------|
| `services/RevenueCatService.ts:45` | `tarot_sub` | Google Play 등록 ID (정상) |
| `services/purchases.ts:23` | `tarot_sub` | 정상 |
| `services/purchasesWeb.ts:9,16` | `tarot_premium_monthly/yearly` | 불일치 |
| `services/PaymentService.ts:40` | `monthly_premium/yearly_premium` | 불일치 |
| `views/SubscriptionView.vue:62,203` | `monthly_premium/yearly_premium` | 불일치 |

**조치**: `tarot_sub`으로 전부 통일 필요

### 2. 가격 불일치
| 파일 | 월간 | 연간 |
|------|------|------|
| `purchases.ts` | ₩2,900 | (없음) |
| `purchasesWeb.ts` | ₩2,900 | ₩29,900 |
| `PaymentService.ts` | ₩4,900 | ₩49,000 |

**조치**: ₩2,900으로 통일 (Google Play Console 기준), 연간 상품 제거

## HIGH - 배포 전 해결 필요

### 3. 웹 결제 미구현
- `purchasesWeb.ts:173-218` — 결제 로직이 모킹 상태, 프로덕션에서 항상 실패 반환
- 현재 모바일 앱 전용이므로 당장 문제 없지만, 웹 접근 시 혼란 유발

### 4. 에러 핸들링 부재
- `RevenueCatService.ts` — 5곳에서 에러를 console.error만 하고 무시
- 초기화/로그인/구매/복원 실패 시 사용자에게 피드백 없음
- 위치: 84, 99, 111, 127, 282번째 줄

### 5. iOS API Key 미설정
- `RevenueCatService.ts:40` — iOS key가 빈 문자열
- 현재 Android 전용이므로 당장 문제 없음

## MEDIUM - 리팩토링 시 해결

### 6. 데드코드
| 파일 | 설명 |
|------|------|
| `services/PaymentService.ts` | 레거시 결제 서비스 (RevenueCat으로 대체됨) |
| `services/purchases.mock.ts` | 사용되지 않는 모의 서비스 |
| `services/purchasesWeb.ts` | 웹 결제 서비스 (TossPayments 미구현) |

### 7. 익명 사용자 코드 잔존
- `store/user.ts:266-290` — `createAnonymousUser()` 함수 존재
- CLAUDE.md 규칙 #9 위반 (익명 사용자는 존재하지 않음)

### 8. 콘솔 로그 과다
- `RevenueCatService.ts` 15+개, `purchasesWeb.ts` 10+개
- 프로덕션 빌드에서 비활성화 필요

### 9. Supabase 민감 정보 로그
- `services/supabase.ts:98-100` — supabaseUrl, supabaseKey 일부가 콘솔에 출력됨

## LOW - 개선 사항

### 10. 유틸리티 중복
- `formatPrice()` 함수가 `purchases.ts`와 `purchasesWeb.ts`에 각각 존재

### 11. 구독 상태 동기화 불일치
- 모바일: RevenueCat → subscription 테이블 → user store
- 웹: profileService.updatePremiumStatus 직접 호출 (subscription 테이블 미사용)

## 권장 조치 순서

1. **상품 ID/가격 통일** → 단일 상수 파일로 관리
2. **PaymentService.ts 삭제** → RevenueCatService로 완전 대체
3. **SubscriptionView.vue 상품 ID 수정** → `tarot_sub` 사용
4. **익명 사용자 코드 제거** → CLAUDE.md 규칙 준수
5. **에러 핸들링 개선** → 사용자 알림 추가
6. **프로덕션 콘솔 로그 정리** → 민감 정보 노출 차단
